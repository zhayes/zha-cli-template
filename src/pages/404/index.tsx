import React, {Component} from 'react';

const NotFound = ({ location }:any) => {
    return (
        <div>
            <h3>
                当前页面不存在
            </h3>
        </div>
    );
}

export default NotFound;